# Contributor: Pegasus Heavy Industries
# Maintainer: Pegasus Heavy Industries
pkgname=bitbucket-cli
pkgver=1.0.0
pkgrel=0
pkgdesc="A powerful command-line interface for Bitbucket Cloud"
url="https://github.com/quinnjr/bitbucket-cli"
arch="all"
license="MIT"
depends="dbus-libs"
makedepends="cargo rust dbus-dev cargo-auditable"
source="$pkgname-$pkgver.tar.gz::https://github.com/quinnjr/bitbucket-cli/archive/v$pkgver.tar.gz"
options="net" # cargo fetch

prepare() {
	default_prepare
	cargo fetch --target="$CTARGET" --locked
}

build() {
	cargo auditable build --release --frozen
}

check() {
	cargo test --frozen
}

package() {
	install -Dm755 target/release/bitbucket "$pkgdir"/usr/bin/bitbucket
	install -Dm644 LICENSE "$pkgdir"/usr/share/licenses/$pkgname/LICENSE
	install -Dm644 README.md "$pkgdir"/usr/share/doc/$pkgname/README.md
}

sha512sums="SKIP"
